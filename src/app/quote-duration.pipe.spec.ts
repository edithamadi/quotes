import { QuoteDurationPipe } from './quote-duration.pipe';

describe('QuoteDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new QuoteDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
